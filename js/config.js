window.BASH_SITE_CONFIG = {
  /* Заполните, чтобы заявки дублировались в Telegram.
     1) @BotFather → токен
     2) напишите боту, затем getUpdates и возьмите chat.id
     Токен в статическом сайте виден в коде — для теста ок, для боя лучше прокси. */
  telegramBotToken: "",
  telegramChatId: "",
  /* Числовой ID счётчика Яндекс Метрики, например 12345678 */
  metrikaId: ""
};
